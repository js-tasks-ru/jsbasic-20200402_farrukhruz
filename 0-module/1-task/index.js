 /**
 * Power
 * @param {number} m base
 * @param {number} n index
 * @returns {number}
 */
function sum(m, n) {
  // ваш код...
  m = prompt('Enter some number for m ?')
  n = prompt('Enter some number for n ?')
  return m * n;
}
console.log(sum())
